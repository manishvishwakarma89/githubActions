name: Hello World Workflow

on: push

jobs:
  hello-job:
    runs-on: nginx-latest

    steps: 
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Print Hello World
        run: echo "Hello, Manish Kumar Vishwakarma from GitHub Actions!
        
